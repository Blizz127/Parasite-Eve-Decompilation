# Phase 6E-B44: `func_8005B91C`

## Verdict

`func_8005B91C` is classification 1, translated retail logic. It is a
deterministic signed table-index and interpolation decomposer. It selects one
of the 0x200-byte tables returned by translated `func_8005DB8C`, performs the
retail binary search, writes the selected signed index through nullable `a2`,
and optionally writes a signed 0..48 interpolation fraction through nullable
`a3`. It has no unresolved dependency and no platform, SDK, callback,
hardware, polling, blocking, host-time, or environment behavior.

B44 therefore translates the entire function. It does not translate
`func_80052F24`. The B43 production prefix can now execute all seven B44
calls and their seven translated `func_8005DBAC` consumers. On paths with a
non-null record holder it stops at `func_80052F24` after executing that call's
retail delay-slot write. The actual Disc 1 boot state takes B43's earlier
null-holder return, so the fresh real-disc strict frontier becomes
`func_80087090` from `func_8006A9E4`; that provider remains untouched.

## Retail identity and complete source census

- Executable start `0x8005B91C`, exclusive end `0x8005BA78`.
- File offset `0x4C11C`, size `0x15C`, exactly 87 instructions.
- Live split `configs/USA/disc1.yaml:345`, `[0x4C0A8, asm]`.
- Body `asm/disc1/4C0A8.s:51-152`; nonmatching declaration is line 49.
- `build/disc1.map:6098-6099` provides the nonmatching and exported symbols.
- No matching/nonmatching C body, C declaration, SDK-map entry, or Psy-Q
  symbol exists. Before B44 the port referenced it only as B43's centralized
  boundary; B43 documentation and tests captured that boundary contract.
- The sole direct callee is translated `func_8005DB8C`.

The executable SHA-1 is
`452fb033f2eaa4b18aa20a5bca60b8125af3a37b`. `tools/b44_oracle.py`
compares all 87 literal words and all 18 executable call sites against that
exact executable before interpreting any instruction. The complete literal
transcription is:

```text
27BDFFE0 AFB00010 00A08021 AFB20018
00C09021 AFB10014 AFBF001C 0C0176E3
00E08821 00403021 24050040 24030020
00051080 00462021 8C820000 00000000
0202102A 14400003 00000000 08016E64
00A32821 8C82FFFC 00000000 0202102A
10400003 00000000 08016E64 00A32823
00001821 00031843 1460FFEE 00051080
24A5FFFF 28A20063 10400002 24030062
00A01821 00602821 28A20062 10400019
00051080 00461021 8C460004 8C440000
00000000 10C40013 02041823 00031040
00431021 00021100 00431021 00C41823
0043001A 14600002 00000000 0007000D
2401FFFF 14610004 3C018000 14410002
00000000 0006000D 00001812 08016E8A
28620031 00001821 28620031 14400002
00000000 24030030 12400002 00000000
AE450000 12200006 28A20062 10400003
24020030 08016E97 AE230000 AE220000
8FBF001C 8FB20018 8FB10014 8FB00010
27BD0020 03E00008 00000000
```

## True ABI and output contract

The semantic retail signature is:

```c
void func_8005B91C(int32_t table_number, int32_t key,
                   pe_addr_t index_out, pe_addr_t fraction_out);
```

`a0` is a signed 32-bit table number whose wrapped low 32 bits feed
`func_8005DB8C`; all executable callers use nonnegative table selectors.
`a1` is a signed 32-bit key. `a2` and `a3` are independently nullable
32-bit output pointers. Neither is read. On every normal path the calculated
index is stored as one word through non-null `a2`. If `a3` is non-null, one
word is stored after the `a2` store: the interpolation fraction when index is
below 98, otherwise exactly 48. Thus aliased outputs are legal and the later
fraction store wins. There is no failure path that leaves a non-null output
untouched.

The body has one epilogue and no contractual return. All 18 callers ignore
`v0`. Its residual value is `(index < 98)` when `a3==0`, and 48 when `a3` is
non-null. The port deliberately exposes `void`, rather than preserving an
unconsumed register accident.

Retail stack output pointers cannot be represented by `pe_addr_t` in native
C. `PE_func_8005B91C_HostOut` is a separate full-width adapter for translated
callers with host stack temporaries. Both public forms use the same arithmetic
core and identical index-before-fraction ordering. No host pointer is
truncated, placed in guest RAM, or confused with a guest address.

## Exact algorithm and arithmetic

Let `base = func_8005DB8C(table_number)`. Search begins with index 64 and
step 32. Each iteration first reads signed `table[index]`:

1. If `key >= table[index]`, add `step` to the index.
2. Otherwise read signed `table[index-1]`. If `key < table[index-1]`,
   subtract `step`; otherwise set `step` to zero.
3. Arithmetic-shift `step` right once and repeat while nonzero.

After the loop retail subtracts one from the index and applies only the
signed upper clamp `index >= 99 -> 98`. There is no lower clamp. Given the
fixed search start/steps, the minimum normal selected index is zero. Search
reads can range from `base` through `base+0x200` inclusive; all are four-byte
reads.

For index below 98 retail reads `high=table[index+1]` before
`low=table[index]`. Equal words yield fraction zero. Otherwise:

```text
delta       = wrap32(key - low)
numerator   = wrap32(delta * 49)       # exact shift/add sequence
denominator = wrap32(high - low)
fraction    = signed numerator / signed denominator, truncating toward zero
fraction >= 49 (signed) -> 48
```

Negative fractions are not clamped. Index 98 produces fraction 48 without
adjacent-table reads. The equality branch makes divide-by-zero unreachable.
Retail nevertheless contains `break 7` for zero and `break 6` for
`INT32_MIN/-1`; production explicitly aborts the latter instead of invoking
C signed-division undefined behavior.

## Complete instruction-order operation map

The ranges below are contiguous, include every word once, and sum to 87.
The literal list above and oracle interpreter provide the per-address raw
word/decode record.

| Address range | Words | ROM-order operation, operands, widths, and ordering |
| --- | ---: | --- |
| `B91C..B93C` | 9 | Allocate 0x20-byte frame; save `s0/s2/s1/ra`; preserve signed key, `a2`, and `a3`; `jal 5DB8C(a0)`; delay copies `a3`. |
| `B940..B94C` | 4 | Save returned guest base; set index 64 and step 32; derive `index<<2`. |
| `B950..B96C` | 8 | Derive current word address; `lw` signed bits; signed `key<current`; branch delay nop; greater/equal jump adds step in its delay. |
| `B970..B98C` | 8 | `lw` prior word; signed compare; branch delay nop; lower jump subtracts step in its delay; between-words path clears step. |
| `B990..B998` | 3 | Arithmetic halve step; loop branch samples nonzero before delay; delay derives the next byte offset exactly once. |
| `B99C..B9AC` | 5 | Decrement selected index; signed `index<99`; branch delay prepares 98; preserve index or clamp to 98. |
| `B9B0..B9BC` | 4 | Copy final index; signed `index<98`; branch samples before delay; delay derives interpolation address. |
| `B9C0..B9D4` | 6 | Derive selected address; read high word first, then low word; equality branch samples before delay; delay always computes wrapped `key-low`. |
| `B9D8..BA1C` | 18 | Exact shift/add `delta*49`; wrapped `high-low`; signed divide; zero-denominator branch and `break 7`; `INT32_MIN/-1` checks and `break 6`; `mflo`; jump delay samples signed `fraction<49`. |
| `BA20..BA30` | 5 | Equal/index-98 path selects zero; signed fraction clamp selects 48 only for values at least 49. |
| `BA34..BA3C` | 3 | Null-check `a2` with nop delay; exactly one 32-bit index store if non-null. |
| `BA40..BA58` | 7 | Null-check `a3`; delay computes `index<98`; index-98 branch delay prepares 48; interpolation path stores fraction in jump delay; other path stores 48. |
| `BA5C..BA74` | 7 | Restore `ra/s2/s1/s0`, release frame, return; nop delay. |

The only call is `jal func_8005DB8C` at `0x8005B938`; its delay at
`0x8005B93C` preserves `a3`. The translated leaf consumes exactly `a0`,
reads guest word `0x800A8038`, and returns
`load32(A8038)+0x800A8028+(a0<<9)`. No B44 state is committed before it and
only that base is required afterward.

All other controls and delay slots are: `B960/nop`, `B968/index+=step`,
`B97C/nop`, `B984/index-=step`, `B994/index<<2`, `B9A4/prepare98`,
`B9B8/index<<2`, `B9D0/key-low`, `B9F0/nop`, `BA00/load INT32_MIN`,
`BA08/nop`, `BA18/fraction<49`, `BA28/nop`, `BA34/nop`,
`BA40/index<98`, `BA48/prepare48`, `BA50/store fraction`, and
`BA70/nop`. Every condition is sampled at issue and every delay executes
once in the independent oracle.

## Every executable caller

There are exactly 18 direct `jal` sites in ten functions. Every site sets
`a3=0`, ignores residual `v0`, and is repeatable whenever its caller path or
loop repeats.

| Caller / site(s) | Exact arguments and derivation | Output ownership and consumer |
| --- | --- | --- |
| `func_800439D8 @43AC4` | Loop `a0=0..6`; `a1` is zero-extended `lhu(C0E28+2*i)` stored/reloaded from `A18D8[i]`; `a2=A18B4+4*i`; delay advances output cursor | Persistent guest index array; following loop condition, no return use |
| `func_80043C64 @43CFC` | `a0=s2` (loop starts 1); `a1=wrap32(A18DC[...] + ((gp+1D0)*A18FC[s2] >> 7))`; `a2=sp+10`; delay clears `a3` | Stack word loaded immediately, incremented, passed to `func_800605F8` |
| `func_80048918 @48D00` | Loop `a0=0..6`; zero-extended `lhu C0E28+2*i` via `A18D8`; `a2=A18B4+4*i`; delay advances cursor | Persistent guest index array; later state calculation consumes it |
| `func_8004A6CC @4A948` | `a0=load32(gp+260)`, `a1=load32(gp+26C)`, `a2=sp+10`; delay clears `a3` | Stack word loaded, incremented, passed to `func_800605F8` |
| `func_8004B70C @4B850` | Loop `a0=0..6`; signed `lh C0E28+2*i`; `a2=A18B4+4*i`; delay stores sign-extended key to `A18D8[i]` | Persistent guest index array, followed by `A18FC[i]` calculation |
| `func_8004B70C @4B8A8` | `a0=0`; `a1=A18D8[0]+A18FC[0]` formed in the call delay; `a2=sp+10`; `a3=0` before call | Stack word loaded and passed to `func_80051DF8` |
| `func_8004BCE8 @4BDA4` | Loop `a0=0..6`; signed `lh C0E28+2*i`; `a2=A18B4+4*i`; delay stores key to `A18D8[i]` | Persistent output; following instruction zeros `A18FC[i]` |
| `func_8004BCE8 @4BDD0` | `a0=0`; `a1=A18D8[0]`; `a2=sp+10`; delay clears `a3` | Output is written but not reread; following operation stores constant state |
| `func_80050D20 @50D94` | `a0=input+1`; `a1=wrap32(A18D8[a0]+((gp+1D0)*A18FC[a0]>>7))`; `a2=sp+10`; delay clears `a3` | Stack output +1 passed to `func_800605F8` |
| `func_80050E70 @50EC4` | `a0=input+5`; `a1=A18EC[input]` supplied in call delay; `a2=sp+10`; `a3=0` | Stack output +1 passed to `func_800605F8` |
| `func_8005218C @521B0,52280,522B4,522E8,52330,52370,523A4` | `a0=0..6`; signed `lh(C0E28+2*i)`; same transient `sp+10`; every delay clears `a3` | Each output is loaded immediately and passed to `5DBAC`, with `A1B34..44` added for calls 2..6; call 7 passes it directly |
| `func_800523F8 @52420` | `a0=3`; zero-extended `lhu C0E2E`; `a2=sp+10`; delay clears `a3` | Stack output plus `A1B3C` passed to `5DBAC` |

The caller JAL delay is observable where noted; none consumes B44's residual
return. Persistent-output loops and all stack-output consumers are covered by
the executable-wide oracle census and production caller-contract tests.

## Seven B43 calls and real Disc 1 output

B43 uses one native local corresponding to retail `sp+0x10`. Calls 1..7
pass table numbers 0..6 and signed halfwords `C0E28..C0E34`. Their exact
subsequent `5DBAC` inputs are `O0`, `O1+A1B34`, `O2+A1B38`,
`O3+A1B3C`, `O4+A1B40`, `O5+A1B44`, and `O6`. Later calls reuse the local,
but each result is consumed before it is overwritten. No call depends on an
earlier B44 return; later B43 state does depend on earlier output-derived
record writes.

For the SHA-exact Disc 1 state, `PE.IMG` is at LBA 1013. Its cycle-A chunk
header at guest `0x800A8028` begins:

```text
0000000B 00000030 00003100 000035E0
000055C0 000063C0 00006D08 00006E98
```

Thus corrected `0x800A8038=0x000055C0`, table 0 base is
`0x800A8028+0x55C0=0x800AD5E8`, its first signed words are
`0,10,20,30,40,50`, and `C0E28` is zero. The first B43 call therefore writes
the exact 32-bit value zero through `a2`. All seven real boot target keys are
zero and all seven selected tables begin at zero, so every B43 `O[i]` is
zero. These values come from the read-only Disc image and executable, not
from production output or the former non-strict boundary.

## Memory footprint and state authority

The fixed direct guest read is the four-byte word `0x800A8038` inside
translated `5DB8C`. Data-dependent four-byte reads span selected
`base..base+0x200`; interpolation additionally reads the selected pair in
upper-then-lower order. There are no executable or rodata reads after native
translation. Direct B44 writes are only one word at non-null `a2`, followed
by one word at non-null `a3`. The guards are the independent null checks;
there is no count, pointer, index, fraction, or address clamp beyond the
retail upper index/fraction comparisons.

| Retail state | Readers/writers | Authority | Reset |
| --- | --- | --- | --- |
| `0x800A8038` | `5DB8C` reads; archive/resource initialization writes | Contiguous guest RAM via checked `PE_LoadU32/PE_StoreU32` | `PE_RamReset` clears; SDK reset preserves |
| Selected table words | B44 reads; archive loading initializes | Contiguous guest RAM | RAM reset clears |
| `a2` / `a3` guest words | B44 writes only | Caller-owned guest RAM; checked stores | Owner/reset follows pointed storage |
| B43 transient output | B44 host adapter writes, B43 immediately reads | Full-width host automatic local only | Per invocation |

Retail `$gp=0x8009CD70`, but B44 has no direct `$gp` instruction. The nested
leaf's signed `lui 0x800B` / `addiu 0x8038` resolves to corrected
`0x800A8038`. Stale `0x800B8034/38` bytes are neither inputs nor outputs.
Address zero remains invalid except where the retail null-output guards skip
access. There is no KUSEG mirror, fallback output, host `$gp` dependency,
pointer truncation, or split-brain copy.

## Verification

`tools/b44_oracle.py` uses one active register file, byte-backed
little-endian memory (including unaligned self-coverage), exact 32-bit signed
arithmetic/division, issue-time branch sampling, and one execution per delay
slot. It logs reads, writes, calls, returns, and output words before/after the
call. It models only translated `5DB8C`, never production C, and verifies the
seven B43 output-to-`5DBAC` contexts plus the exact real Disc 1 first output.

Ten new production tests cover the true signature, nullable outputs, every
search/fraction path, negative and high results, exact word widths, aliased
write order, the signed division trap, all 18 call sites, all seven B43
contexts, dirty/repeated/reset behavior, full 2 MiB canary footprints, stale
alias and low-address rejection, full-width host outputs, B43 state
integration, and strict advancement. Together with the ten updated B43 tests,
the normal and fresh ASan/UBSan suites pass 427/427, and all 29 oracle
programs pass. Normal and sanitizer strict runs agree on
`func_80087090` from `func_8006A9E4`; bootstrap strict remains
`func_8007F72C` from `func_800698D4`. Three-run determinism preserves the
canonical framebuffer and trace hashes and real-disc FNV-1a-64, and the
isolated Docker matching rebuild remains exact at SHA-1
`452fb033f2eaa4b18aa20a5bca60b8125af3a37b`.
