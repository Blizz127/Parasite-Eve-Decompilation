# Phase 6E-B53G — `func_800746A0` DMA callback-slot setter

## Verdict and scope

B53G translates the complete Psy-Q DMA callback-slot setter
`func_800746A0`, which completes the B53F wrapper `func_80073CF4`. With the
registration result available, the canonical `func_80076C34` enqueue path
proceeds through its literal ring construction and publication and stops at
the untranslated retail queue pump:

```text
func_80076EE4
called from func_80076C34
```

B53G does **not** translate `func_80076EE4`, deliver a DMA interrupt,
service or complete the still-active first LoadImage DMA, dispatch a DMA
IRQ, invoke a callback, move the consumer, pump the ring, run DrawSync
callback delivery, or advance VBlank. Registration is not delivery.

Starting B53F commit:

```text
0650bdbef128b82c44faae5d378acdcbd8bfcf96
```

## Retail body

The SHA-exact retail executable is:

```text
SHA-1 452fb033f2eaa4b18aa20a5bca60b8125af3a37b
```

| Property | Value |
| --- | --- |
| Start | `0x800746A0` |
| Inclusive last byte | `0x8007474B` |
| Exclusive end | `0x8007474C` |
| Executable file offset | `0x64EA0` |
| Size | `0xAC` / 172 bytes |
| Instructions | 43 |
| Live split | `asm/disc1/64CC8.s` |
| Body SHA-256 | `ac160079410a40d719e5a8668f627d05d36d287d08959adc22fd3e069e4e99dd` |

The prior B53A estimate of `0xAC` is confirmed independently: the next
symbol is `func_8007474C`, whose prologue word `0x10A00006` is verified at
the exclusive end.

All 43 words were compared with the executable:

```text
800746A0  00803021  addu   a2,a0,zero      ; a2 = channel
800746A4  3C038009  lui    v1,0x8009
800746A8  246356C0  addiu  v1,v1,0x56C0    ; &D_800956C0
800746AC  00061080  sll    v0,a2,2         ; channel * 4
800746B0  00431821  addu   v1,v0,v1        ; raw slot address
800746B4  8C670000  lw     a3,0(v1)        ; previous handler
800746B8  00A02021  addu   a0,a1,zero      ; a0 = handler
800746BC  10870021  beq    a0,a3,0x80074744
800746C0  00E01021   addu  v0,a3,zero      ; delay slot: return previous
800746C4  10800010  beqz   a0,0x80074708
800746C8  3C0200FF   lui   v0,0x00FF       ; delay slot
800746CC  3C058009  lui    a1,0x8009
800746D0  8CA556BC  lw     a1,0x56BC(a1)   ; DICR pointer word
800746D4  3442FFFF  ori    v0,v0,0xFFFF    ; mask 0x00FFFFFF
800746D8  AC640000  sw     a0,0(v1)        ; slot = handler
800746DC  8CA40000  lw     a0,0(a1)        ; DICR read
800746E0  24C30010  addiu  v1,a2,0x10      ; channel + 16
800746E4  00822024  and    a0,a0,v0
800746E8  24020001  addiu  v0,zero,1
800746EC  00621004  sllv   v0,v0,v1        ; 1 << ((channel+16) & 31)
800746F0  3C030080  lui    v1,0x0080
800746F4  00431025  or     v0,v0,v1
800746F8  00822025  or     a0,a0,v0
800746FC  ACA40000  sw     a0,0(a1)        ; DICR write
80074700  0801D1D1  j      0x80074744
80074704  00E01021   addu  v0,a3,zero      ; delay slot: return previous
80074708  3C058009  lui    a1,0x8009
8007470C  8CA556BC  lw     a1,0x56BC(a1)   ; DICR pointer word
80074710  3442FFFF  ori    v0,v0,0xFFFF    ; mask 0x00FFFFFF
80074714  AC600000  sw     zero,0(v1)      ; slot = 0
80074718  8CA30000  lw     v1,0(a1)        ; DICR read
8007471C  24C40010  addiu  a0,a2,0x10      ; channel + 16
80074720  00621824  and    v1,v1,v0
80074724  3C020080  lui    v0,0x0080
80074728  00621825  or     v1,v1,v0        ; master set BEFORE the clear
8007472C  24020001  addiu  v0,zero,1
80074730  00821004  sllv   v0,v0,a0
80074734  00021027  nor    v0,zero,v0
80074738  00621824  and    v1,v1,v0
8007473C  ACA30000  sw     v1,0(a1)        ; DICR write
80074740  00E01021  addu   v0,a3,zero      ; return previous
80074744  03E00008  jr     ra
80074748  00000000   nop                   ; jr delay slot
```

There are two conditional branches, one direct jump, and one return. Every
delay slot is literal and verified: `addu v0,a3,zero` after the equality
branch, `lui v0,0x00FF` after the null-handler branch, `addu v0,a3,zero`
after the install-path jump, and an exact `nop` after `jr ra`. There is no
`jal`, no `jalr`, and no stack frame.

## ABI

```c
pe_addr_t func_800746A0(uint32_t dma_channel, pe_addr_t handler);
```

Both arguments and the return are 32-bit retail values. `handler` and the
returned previous handler are guest function identities, never native
function pointers and never booleans. There is no third argument: `a2` is
overwritten by the first instruction and `a3` by the fifth.

## Caller census

An executable-wide scan of the SHA-exact image finds:

| Reference kind | Count | Sites |
| --- | --- | --- |
| Direct `jal func_800746A0` (`0x0C01D1A8`) | 0 | — |
| Static data word equal to `0x800746A0` | 0 | — |
| `lui`/`addiu` address materialization | 1 | `0x80074508` in `func_800744D4`, into `v0`, returned |

`func_800746A0` is therefore reached **only** indirectly. `func_800744D4`
returns its identity; the first guard-passing `ResetCallback`
(`func_80073E28` → `func_8007436C` at `0x80073EDC`) stores that identity in
libetc jump-table field `+0x04` in the `0x80073EE0` delay slot. Exactly one
executable wrapper dispatches that field:

| Wrapper | Jump-table field |
| --- | --- |
| `0x80073C94` | `+0x0C` |
| `0x80073CC4` | `+0x08` |
| **`0x80073CF4`** | **`+0x04`** |
| `0x80073D24` | `+0x14` |
| `0x80073D58` | `+0x14` |
| `0x80073D88` | `+0x10` |
| `0x80073DB8` | `+0x18` |

The effective caller contract is therefore B53F's 11 `func_80073CF4` call
sites, which use channels 2, 3, and 4 with full-width guest handlers,
including null-handler removal at `0x80074C94` and `0x80076F90`. The
production translation implements that whole ABI; it implements IRQ
delivery for no channel.

## Callback table authority

| Property | Value |
| --- | --- |
| Base | `D_800956C0` = `0x800956C0` |
| Slots | 8 |
| Entry size | 4 bytes (32-bit guest identity) |
| Extent | `0x800956C0..0x800956DF` |
| Channel → index | `base + (channel << 2)`, unchecked |
| Retail image contents | all eight words zero |
| Next symbol | `D_800956E0` = `0x1F801080` (DMA0 register block) |

Initialization owner is `func_800744D4`, which calls
`func_8007474C(&D_800956C0, 8)` — the already-matched word-memset leaf — and
then zeroes DICR through `D_800956BC` before installing `func_80074520` on
interrupt source 3.

Writers are `func_800746A0` only. The sole reader is the DMA IRQ dispatcher
`func_80074520`, which walks slots 0..6 and `jalr`s a nonzero entry.

The table stays **guest-backed**. B53G introduces no native callback-table
mirror, stores no native function pointer in guest state, and does not
merge this table with the VBlank callback table `D_8009568C` owned by
`pe_callback`. There is exactly one authority.

## Channel range and index contract

Retail performs **no** channel validation of any kind — no minimum, no
maximum, no signedness test, no masking, no rejection. The measured
consequences, reproduced exactly and covered by tests and the oracle:

| Channel | Slot address | Enable bit | Behaviour |
| --- | --- | --- | --- |
| 0..6 | `0x800956C0 + 4*ch` | `1 << (ch+16)` | in-table, dispatchable |
| 7 | `0x800956DC` | `1 << 23` = master | in-table, never dispatched |
| 8 | `0x800956E0` | `1 << 24` (a W1C flag) | one word past the table |
| `0xFFFFFFFF` | `0x800956BC` (DICR pointer) | `1 << 15` | wraps below the table |

`sllv` uses only the low five bits of `channel + 16`, so the shift amount
is `(channel + 16) & 31`. Address arithmetic is exact 32-bit wrapping
`sll`/`addu`.

The native translation reproduces this arithmetic verbatim and reads/writes
through the existing guest-RAM authority. A channel whose computed slot
falls outside the modeled 2 MiB RAM is an access this port cannot
represent; the guest-memory authority reports it rather than corrupting
host state. No executable caller produces one.

## Previous-handler semantics

Every return path is `addu v0,a3,zero` — the full 32-bit word read from the
slot before any decision. No normalisation and no truncation.

| Case | Slot store | DICR access | Return |
| --- | --- | --- | --- |
| empty → install | `slot = handler` | read + write | `0` |
| existing → replace | `slot = handler` | read + write | previous |
| existing → remove (`handler == 0`) | `slot = 0` | read + write | previous |
| zero → zero | none | none | `0` |
| same-handler reinstall | none | none | that handler |
| invalid channel | same as above at the raw address | read + write | word at that address |

The `beq a0,a3` at `0x800746BC` makes both no-op cases complete no-ops:
they perform no guest write and do not touch DICR at all.

## DICR read/modify/write

`D_800956BC` is a retail `.data` word holding the literal `0x1F8010F4`. The
setter loads that pointer and dereferences it; the register itself is the
B53B `pe_gpu` authority, reached through `PE_GPU_ReadDICR` /
`PE_GPU_WriteDICR`. No second DICR global, guest-RAM mirror, or duplicate
enable/pending state is created, and `PE_GPU_SetDMA2InterruptEnabled`
(channel-2-specific) is deliberately not used, because retail derives the
bit from the channel.

Install (`handler != 0`, `handler != previous`):

```text
DICR = (DICR & 0x00FFFFFF) | (1 << ((channel+16) & 31)) | 0x00800000
```

Removal (`handler == 0`, `previous != 0`):

```text
DICR = ((DICR & 0x00FFFFFF) | 0x00800000) & ~(1 << ((channel+16) & 31))
```

Both paths unconditionally set master-enable bit 23. On the removal path
the master OR happens **before** the channel-enable clear, so channel 7 —
whose enable bit *is* bit 23 — clears the master bit again. That asymmetry
is preserved rather than normalised.

The `0x00FFFFFF` mask means the written word carries zeros in bits 24..30,
so the W1C completion flags are never acknowledged by registration. A
pending channel-2 completion flag survives both an install and a removal;
this is verified against a genuine `PE_GPU_ServiceDMA2Completion` flag, not
a synthesised one. Unrelated lower control bits are preserved verbatim.

For the canonical call the measured result is exactly `0x00840000`
(master bit 23 + channel-2 enable bit 18) from an initial `0`.

## DICR bit 31 — verdict

**Verdict B: bit 31 is read and immediately discarded; it is never tested
and never written as one.**

The `lw` at `0x800746DC` / `0x80074718` reads the raw 32-bit word, and the
very next `and` with `0x00FFFFFF` clears bit 31 in the value written back.
No instruction in the body compares, branches on, or otherwise consumes it.
On PSX hardware bit 31 is the read-only derived master flag, so writing
zero there is inert.

The oracle proves the independence directly: the identical scenario run
against a DICR model that *does* synthesize bit 31 and one that does not
produces byte-identical returns, guest writes, and DICR writes.

Consequently **B53B is not extended**. Its deliberate omission of a
synthesized raw bit 31 remains correct and unobservable by this path. Bit
31 *is* consumed elsewhere — `func_80074520` compares `DICR & 0xFF000000`
against `0x80000000` and tests bit 15 for its error report — but that is
the DMA IRQ dispatcher, which B53G does not translate.

## I_MASK — verdict

**`func_800746A0` does not access I_MASK or I_STAT.** The body embeds no
`0x1F801074` or `0x1F801070` literal and performs no MMIO access other than
the two DICR operations; the oracle asserts both. The retail DMA
interrupt-source installation belongs to `func_800744D4`'s
`func_80073CC4(3, func_80074520)` call, which remains the pre-existing
deferred item recorded by B53F.

The single 16-bit `pe_irq` authority from B53D is therefore untouched by
registration, and tests assert the mask is unchanged across it. The only
I_MASK activity on the canonical path is `func_80076C34`'s own exchange and
its retail restore, which happens strictly after producer publication and
is not suppressed by any host frame-limit or run-control state.

## Operation ordering

Recovered from the literal words, not assumed:

1. compute the raw slot address (no validation);
2. read the previous handler;
3. compare against the new handler and return immediately if equal;
4. branch on `handler == 0`;
5. **store the slot** (`0x800746D8` / `0x80074714`);
6. **then** read DICR (`0x800746DC` / `0x80074718`);
7. compute and write DICR;
8. return the previous handler.

The slot store strictly precedes the DICR read on both paths; the oracle
checks this from the execution trace. No interrupt delivery occurs inside
the function, so registration is atomic from the native port's perspective
without inventing concurrency.

## Canonical entry and exit state

Fresh normal and sanitizer captures immediately before the setter measured
`channel = 2`, `handler = 0x80076EE4`, callback table all zero,
`DICR = 0`, `DPCR = 0x33333B33`, `GPUSTAT = 0x04000000`,
`MADR = 0x8012A8B8`, `BCR = 0x00400010`, `CHCR = 0x01000201`,
DMA2 active = 1, completion pending = 0, `I_MASK = 0`, producer = 0,
consumer = 0, VBlank = 0.

Immediately after the setter and the enqueue that follows it:

| Item | Before | After |
| --- | --- | --- |
| callback slot 2 | `0` | `0x80076EE4` |
| DICR | `0x00000000` | `0x00840000` |
| setter return | — | `0` |
| DMA2 active | 1 | 1 |
| DMA2 completion pending | 0 | 0 |
| DMA2 interrupt asserted | 0 | 0 |
| MADR / BCR / CHCR | `0x8012A8B8` / `0x00400010` / `0x01000201` | unchanged |
| GPUSTAT | `0x04000000` | `0x04000000` |
| VBlank | 0 | 0 |
| DPCR | `0x33333B33` | `0x33333B33` |
| producer | 0 | 1 |
| consumer | 0 | 0 |
| I_MASK | 0 | 0 (exchanged and restored) |

The pending first LoadImage DMA is still active and still incomplete.

## `func_80073CF4` is now complete

The 12-word wrapper no longer stops at a boundary: it dispatches the
installed identity and forwards the setter's full 32-bit `v0`. B53F's
prefix-only status is resolved by B53G; B53F history is not amended.
Pre-install and dirty jump-table behaviour remains deliberately
unrepresented, since no executable path reaches the wrapper before the
ResetCallback install chain runs.

## Queue publication

Canonical execution reaches ring publication, so `func_80076C34`'s literal
enqueue block `0x80076D68..0x80076EA0` is translated. It has no unresolved
dependency: its only call is the already-translated `func_80073E10`.

Ring geometry, proven from the executable words: 64 entries of 96 bytes at
`D_800BD030`, with `+0x00` worker identity, `+0x04` argument, `+0x08`
auxiliary, and the copied inline payload from `+0x0C`. Entry offset is
`producer * 96` (`sll`/`addu`/`sll` at `0x80076DB0..0x80076DB8`).

The published second LoadImage request is exactly:

| Field | Value |
| --- | --- |
| worker (`+0x00`) | `0x80076664` |
| argument (`+0x04`) | `0x800BD03C` — guest address of the copied payload |
| auxiliary (`+0x08`) | `0x8012B8B8` — source guest address |
| payload (`+0x0C`, `+0x10`) | `0x01C80100`, `0x00010040` = RECT `{256,456,64,1}` |
| producer | `0` → `1` |
| consumer | `0` (unmoved) |

The payload is copied before publication, the entry is complete before the
producer advances, and the producer advances in the `0x80076EA0` delay slot
— strictly before the I_MASK restore. No native RECT pointer is stored, and
no host queue exists.

The `0x80076D80` word count is signed (`bgez`/`addiu 3`/`sra 2` then a
signed `slt`), so a negative byte count copies nothing and only the
metadata words are written; that path is covered by tests.

Retail performs no bounds check on the copy source or the entry. A span
this port cannot represent — an out-of-RAM copy source, or a copy wider
than the 84-byte payload area — becomes a visible honest boundary
(`func_80076C34_enqueue_span`) after the registration retail had already
performed, and before any publication. This mirrors B53E's source-span
preflight rather than aborting the host or silently narrowing the copy.

## Strict advancement

| Run | Before B53G | After B53G |
| --- | --- | --- |
| canonical Disc 1 strict (normal) | `func_800746A0` from `func_80073CF4`, exit 1 | `func_80076EE4` from `func_80076C34`, exit 1 |
| canonical Disc 1 strict (ASan/UBSan) | same | same |
| bootstrap strict (normal and sanitizer) | `func_8007F72C` from `func_800698D4`, exit 1 | unchanged |

`func_80076EE4` is the retail queue pump and is explicitly out of scope.

## Oracle and tests

`tools/b53g_oracle.py` is independent of production C. It verifies the
executable SHA-1, the file offset, the exact range, the body SHA-256, all
43 literal words, both branch targets, the jump target, all four delay
slots, and the absence of any call. It then executes those words on a
MIPS-I interpreter with exact delay slots, against guest RAM plus a DICR
model with PSX W1C flag semantics and an optional read-only master flag.
It covers the callback-table indexing, all eight in-range channels, the
above-range and negative channels, first install, replacement, removal,
zero-to-zero, same-handler reinstall, full-width previous-handler returns,
both DICR RMW expressions, unrelated control-bit and completion-flag
preservation, the channel-7 master asymmetry, the explicit bit-31
independence proof, the absence of any non-DICR MMIO access, the
whole-executable caller census, the initialization owner, and the canonical
`func_80073CF4` call context. Mutation probes confirm it fails on a
corrupted literal word, a wrong master bit, a changed wrapper field, and a
broken W1C model.

Seven focused production tests cover the exact ABI and executable caller
channels, the channel-range contract including below/above range, the full
handler lifecycle, the exact DICR install/removal RMW with a genuine
pending completion flag, registration-is-not-delivery (full-RAM canary,
VRAM samples, GPU/DMA/VBlank/I_MASK inertia), reset lifecycle and repeated
determinism, and the new strict frontier. Four B53F tests and three
B53C/B53D tests were retargeted from the removed boundary to the measured
registration and publication facts.

## Verification

- normal native suite: 517/517;
- fresh ASan/UBSan suite: 517/517, no sanitizer diagnostics;
- B53G oracle: PASS;
- B53F/B53E/B53D/B53C/B52/B51/corrected-B50 oracles: PASS;
- all 33 retained standalone oracles: PASS (40/40 executable oracles total,
  plus `b21_bzero_oracle.py`);
- frozen B53B platform tests: 15/15 in normal and sanitizer builds;
- retained B49 host-loop harness: PASS in normal and sanitizer builds;
- canonical strict normal and sanitizer:
  `func_80076EE4` from `func_80076C34`, exit 1;
- bootstrap strict normal and sanitizer:
  `func_8007F72C` from `func_800698D4`, exit 1;
- framebuffer SHA-256 (bounded normal, sanitizer, and bootstrap):
  `fb28dc21dd1e41eb72b8fe22dd3295bb8ed0c040aa88f7885a68dedc2629dfdb`;
- real-data FNV-1a64: `7D860391E1ED6C97` in normal and sanitizer;
- fresh matching build: exact SHA-1
  `452fb033f2eaa4b18aa20a5bca60b8125af3a37b` (227 C leaves).

## Files and next rung

B53G changes exactly these files:

- `pc_port/game/boot/func_800746A0_port.c` (new complete setter);
- `pc_port/game/boot/func_80073CF4_port.c` (wrapper completed);
- `pc_port/game/boot/func_80076C34_port.c` (enqueue construction and
  publication to the pump boundary);
- `pc_port/tools/b53g_oracle.py` (new independent oracle);
- `pc_port/docs/b53g_func_800746A0.md` (this evidence document);
- `pc_port/CMakeLists.txt` and `pc_port/include/psx_compat.h` (build/API
  wiring);
- `pc_port/tests/test_native.c` (seven new tests, seven retargeted);
- `pc_port/tools/b49_host_loop_test.py` (frontier gate);
- `pc_port/README.md`, `pc_port/docs/shim_inventory.md`, and
  `docs/ai_context/ACTIVE_HANDOFF.md` (measured state).

No B53B GPU/DMA implementation is changed.

The next bounded task is `func_80076EE4` from `func_80076C34`: the retail
queue pump. It must be recovered before it can decide whether the pending
DMA is serviced, and it must not be reached by servicing DMA to get there.

Two prerequisites remain explicitly deferred: full pre-ResetCallback/dirty
SDK jump-table behaviour, and the retail DMA IRQ source-3/I_MASK
installation omitted by the collapsed reset path (`func_80073CC4(3,
func_80074520)`). Neither is observed by the registration-only boundary.
