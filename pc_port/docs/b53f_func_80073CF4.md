# Phase 6E-B53F — `func_80073CF4` installed-target wrapper prefix

## Verdict and scope

B53F translates only the execution-proven installed-target path through
`func_80073CF4` and stops inside the wrapper at its first genuine retail
dependency:

```text
func_800746A0
called from func_80073CF4
```

The rung does not translate `func_800746A0`, register a DMA callback, write
DICR, publish the GPU command ring, invoke `func_80076EE4`, service DMA2, or
deliver an interrupt. The B53E DMA therefore remains active and incomplete.

Starting B53E commit:

```text
f7772f014d037209dfcc1a1d56d3c576593648a6
```

## Retail body

The SHA-exact retail executable is:

```text
SHA-1 452fb033f2eaa4b18aa20a5bca60b8125af3a37b
```

`func_80073CF4` is found in `asm/disc1/5F3E4.s` and
`build/disc1.map` with this exact extent:

| Property | Value |
| --- | --- |
| Start | `0x80073CF4` |
| Inclusive last byte | `0x80073D23` |
| Exclusive end | `0x80073D24` |
| Executable file offset | `0x644F4` |
| Size | `0x30` / 48 bytes |
| Instructions | 12 |
| Body SHA-256 | `3f439194730111b8a1dd4fc5a252bc381c5a52aa4e2b14e34066f6b403749694` |

All words were compared with the executable:

```text
80073CF4  3C028009  lui    v0,0x8009
80073CF8  8C42566C  lw     v0,0x566C(v0)
80073CFC  27BDFFE8  addiu  sp,sp,-0x18
80073D00  AFBF0010  sw     ra,0x10(sp)
80073D04  8C420004  lw     v0,4(v0)
80073D08  00000000  nop
80073D0C  0040F809  jalr   v0
80073D10  00000000  nop
80073D14  8FBF0010  lw     ra,0x10(sp)
80073D18  27BD0018  addiu  sp,sp,0x18
80073D1C  03E00008  jr     ra
80073D20  00000000  nop
```

There are no branches, loops, direct calls, stores outside the private
stack frame, or MMIO operations. The `jalr` and `jr` delay slots are both
`nop`. The indirect callee's `v0` survives the epilogue unchanged.

## ABI and installed target

The execution-relevant ABI is:

```c
pe_addr_t func_80073CF4(uint32_t dma_channel, pe_addr_t handler);
```

Both arguments and the return are 32-bit retail values. `handler` is a guest
function identity, never a native function pointer.

The retail wrapper loads `D_8009566C = 0x8009564C`, then the field at
`0x8009564C + 4`. That field is initially zero in the executable. The first
guard-passing ResetCallback path installs the backend as follows:

1. `func_800744D4` returns guest identity `0x800746A0`;
2. `func_80073E28` calls `func_8007436C` at `0x80073EDC`;
3. its `0x80073EE0` delay slot stores the returned identity at jump-table
   field `+4`.

The native ResetCallback already collapses the SDK jump-table plumbing, as
does the older translated `func_80073D24` wrapper. B53F therefore binds the
proven post-ResetCallback field to numeric guest identity `0x800746A0` at
the instant of dispatch. It does not materialize a host function pointer or
claim pre-ResetCallback/dirty jump-table behavior.

## Caller census

The executable contains exactly 11 direct calls and no data-pointer use of
`func_80073CF4`:

| Caller / call site | `a0`, `a1` | Delay slot | Return use |
| --- | --- | --- | --- |
| `func_80074C14` / `0x80074C94` | `2`, `0` | publish init byte | overwritten |
| `func_80076C34` / `0x80076D60` | `2`, `0x80076EE4` | `a0=2` | ignored |
| `func_80076EE4` / `0x80076F90` | `2`, `0` | `a1=0` | ignored |
| `func_800775E8` / `0x80077698` | `2`, `0x80077A00` | `a0=2` | ignored |
| `func_800776D4` / `0x80077784` | `2`, `0x80077A00` | `a0=2` | ignored |
| `func_800777C0` / `0x80077878` | `2`, `0x80077A00` | `a0=2` | ignored |
| `func_80077904` / `0x800779C8` | `2`, `0x80077A00` | `a0=2` | ignored |
| `func_80077A00` / `0x80077A10` | `2`, `0x80076EE4` | `a0=2` | forwarded |
| `func_8007A8EC` / `0x8007A8F8` | `3`, incoming identity | `a0=3` | forwarded |
| `func_8007DD14` / `0x8007DD20` | `4`, incoming identity | `a0=4` | forwarded |
| `func_800824F0` / `0x800824FC` | `3`, incoming identity | `a0=3` | forwarded |

The native ABI therefore retains channel values 2, 3, and 4 plus full-width
guest handler and return identities, even though the canonical caller
ignores the return.

## Canonical Disc 1 entry state

Fresh normal and sanitizer builds both first reached the wrapper from
`func_80076C34`. A GDB capture immediately before that call measured:

```text
a0 / channel             2
a1 / handler             0x80076EE4
second RECT              {256,456,64,1}
second source            0x8012B8B8
producer                 0
consumer                 0
DMA callback slot 2      0
GPU work marker          1
saved/current I_MASK     0 / 0
GPUSTAT                  0x04000000
DMA2 MADR                0x8012A8B8
DMA2 BCR                 0x00400010
DMA2 CHCR                0x01000201
DPCR                      0x33333B33
DICR                      0
DMA2 active/completion   1 / 0
```

The first LoadImage DMA is still active, so the initialized empty ring uses
the enqueue branch. The call occurs before payload copy, metadata stores, or
producer publication.

## Production boundary

`game/boot/func_80073CF4_port.c` represents the installed backend as
`pe_addr_t 0x800746A0`, forwards the two meaningful arguments through the
centralized indirect-boundary recorder, forwards a controlled 32-bit return
for unit evidence, and requests an unresolved host stop. The dispatcher now
calls this real wrapper instead of recording `func_80073CF4` as a stub.

Because `func_800746A0` has not returned in retail control flow, production
does not execute any of the following after the boundary:

- copied-payload or queue-entry stores;
- producer publication;
- saved-I_MASK restore;
- `func_80076EE4` pump;
- callback invocation or DMA completion.

## Separate callback authority

`func_800746A0` is a separate 43-instruction / `0xAC` body at
`0x800746A0..0x8007474B`, with body SHA-256
`ac160079410a40d719e5a8668f627d05d36d287d08959adc22fd3e069e4e99dd`.
It owns the deferred work:

- guest DMA callback table `D_800956C0..D_800956DC` (eight 32-bit guest
  identities; slot 2 is `0x800956C8`);
- store-if-changed and previous-handler return;
- DICR master/channel-enable RMW after the slot store.

This table is not the VBlank callback table `D_8009568C`, and B53F does not
reuse `pe_callback` for it. B53B `pe_gpu` remains the sole DICR hardware
authority and owns no callback identity.

For the canonical call, a later exact backend translation would change slot
2 from zero to `0x80076EE4`, produce bounded DICR state `0x00840000`, and
return zero. Those effects are evidence for the next rung, not B53F effects.

## Oracle and tests

`tools/b53f_oracle.py` is independent of production C. It verifies:

- executable SHA-1 and all 12 literal words;
- exact range, body SHA-256, next word, `jalr`, and both delay slots;
- all 11 executable caller sites, their argument setup, and call delay slots;
- the ResetCallback install chain to `0x800746A0`;
- channel 2 pump/null/restore identities;
- channel 3/4 and high 32-bit guest identities;
- controlled full-width return forwarding;
- the honest pre-install zero-target boundary;
- no callback, DICR, ring, or hardware progress.

Four focused native tests cover exact ABI/return forwarding, full guest and
hardware state inertia, strict boundary identity, and the pending-DMA
dispatcher integration with ring/callback/DMA canaries.

## Verification

- normal native suite: 510/510;
- fresh ASan/UBSan suite: 510/510, no sanitizer diagnostics;
- B53F oracle: PASS;
- B53E/B53D/B53C/B52/B51/corrected B50 oracles: PASS;
- all 33 retained standalone oracles: PASS;
- frozen B53B platform tests: 15/15 in normal and sanitizer builds;
- retained B49 host-loop harness: PASS in normal and sanitizer builds;
- canonical strict normal and sanitizer:
  `func_800746A0` from `func_80073CF4`, exit 1;
- bootstrap strict normal and sanitizer:
  `func_8007F72C` from `func_800698D4`, exit 1;
- framebuffer SHA-256:
  `fb28dc21dd1e41eb72b8fe22dd3295bb8ed0c040aa88f7885a68dedc2629dfdb`;
- real-data FNV-1a64: `7D860391E1ED6C97` in normal and sanitizer;
- fresh matching build: exact SHA-1
  `452fb033f2eaa4b18aa20a5bca60b8125af3a37b` (227 C leaves).

## Files and next rung

B53F changes exactly these files:

- `pc_port/game/boot/func_80073CF4_port.c` (new installed-target wrapper
  prefix);
- `pc_port/tools/b53f_oracle.py` (new literal/caller/install-chain oracle);
- `pc_port/docs/b53f_func_80073CF4.md` (this evidence document);
- `pc_port/CMakeLists.txt` and `pc_port/include/psx_compat.h` (build/API
  wiring);
- `pc_port/game/boot/func_80076C34_port.c` (call the translated prefix);
- `pc_port/tests/test_native.c` (four focused tests and frontier updates);
- `pc_port/tools/b49_host_loop_test.py` (retained frontier gate);
- `pc_port/game/boot/func_8006AD40_port.c` and
  `pc_port/game/boot/func_8006E1C0_port.c` (measured frontier comments);
- `pc_port/README.md`, `pc_port/docs/shim_inventory.md`, and
  `docs/ai_context/ACTIVE_HANDOFF.md` (measured state).

No B53B GPU/DMA implementation is changed.

The next bounded task is `func_800746A0` from `func_80073CF4`. It should
translate only the guest-backed DMA callback-slot setter and its exact DICR
RMW against the existing B53B authority. It must not translate the queue
pump, dispatch pending DMA IRQs, complete DMA, or merge DMA callbacks with
the VBlank callback model.

Two prerequisites remain explicitly deferred until their execution paths
require them: full pre-ResetCallback/dirty SDK jump-table behavior and the
retail DMA IRQ source-3/I_MASK installation omitted by the collapsed reset
path. Neither is observed by the wrapper-only canonical boundary.
