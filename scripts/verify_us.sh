#!/usr/bin/env bash
# Rebuild the executable from split output and compare its checksum against
# the recorded original. This is the gate for all "matching" claims.
# PLACEHOLDER — implemented in Phase 4.
set -euo pipefail

echo "ERROR: verify_us.sh is a Phase 0 placeholder and not implemented yet." >&2
echo "Until this passes, no matching claims are permitted (see CLAUDE.md)." >&2
exit 1
