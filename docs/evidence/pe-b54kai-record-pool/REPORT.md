# PE-B54K-AI — movie record-pool initializer

Status: **VERIFIED AND INTEGRATED ON THE NATIVE GRIND LANE**.

This rung translates the complete main-executable initializer called by the
movie overlay after `DecDCToutCallback`. It assigns no SDK name: the evidence
proves a 32-byte record pool, but not a public PsyQ symbol.

## Retail identity and function-hood

```text
entry              [0x8007A214,0x8007A240) / 11 words
entry file offset  0x6AA14
entry SHA-256      71f85f940c5697918f4902822149f70490cfa4f66d388530e7fee3c3945ce986
worker             [0x8007A244,0x8007A2A4) / 24 words
worker file offset 0x6AA44
worker SHA-256     be9001c4af959d678340c4d233f6f8d169420493ddde920f7e86e483c284f78d
clear helper       [0x8007C444,0x8007C478) / 13 words
clear file offset  0x6CC44
clear SHA-256      04b4534bd4721b1dfdc3bf42335c54cd9d4b3342d5387c2f52da9ffb26e847c4
production call    0x80192748 jal 0x8007A214
caller prefix      [0x801924F8,0x80192750) / 150 words
caller SHA-256     cddb140d8d927057b46a04a2e22afeafce3e24bc1d1faf13c6599302a035313c
```

Function-hood does not depend on an autogenerated label. The entry has an
exact production `jal`, a conventional prologue, a call to the exact worker,
and `jr ra` plus `nop`. The worker and helper each have exact callers and
canonical returns. A single alignment `nop` at `0x8007A240` separates entry
and worker; it is not included in either function.

## Proven contract

The 11-word entry stores:

```text
D_800C0DC8 = a0  record base
D_800C20C4 = a1  unsigned record count
```

It then calls the 24-word worker. The worker clears four 32-bit state globals,
calls the helper with first index zero and the published count, then clears
two more words and one halfword. The 13-word helper proves the geometry:

```text
record_address = D_800C0DC8 + ((first + index) << 5)
*(uint32_t *)record_address = 0
```

The loop uses `sltu`, so count is unsigned. It writes only offset zero of each
32-byte record. Count zero performs no record access. In production:

```text
a0 = *(uint32_t *)0x801D0DFC
a1 = 0x40
```

The next instruction at `0x80192750` is `li a0,1`, proving the new boundary is
real code. The call is not `ClearOTagR`: the executable's libGPU
`ClearOTagR` wrapper and dispatch target are separate functions with separate
evidence.

## Controls

- Three synthetic records begin dirty; only word zero is cleared.
- Word `+4` in every record and a fourth out-of-count record remain intact.
- The halfword reset preserves the neighboring upper halfword.
- All seven associated globals are poisoned and checked at their retail
  access widths.
- A second call with count zero publishes zero but leaves record memory
  unchanged.
- The real Disc 1 path publishes `D_801D0DFC` and count `0x40` before the
  strict cut.

## Verification

```text
B54K-AI independent oracle: PASS
B54K-AI focused test:       1/1 PASS
B54K-Y real-disc tests:     2/2 PASS
native suite:               991/991
normal CTest:               2/2 PASS
fresh ASan/UBSan CTest:     2/2 PASS
strict real-disc exit:      1
strict frontier:            func_801924F8_80192750_cut
retail EXE SHA-1:           452fb033f2eaa4b18aa20a5bca60b8125af3a37b
```

No callback is invoked, no DMA or MDEC operation is completed, and no scene,
scheduler, story, persistence, or destination state is planted.

```text
FUNC_8007A214=COMPLETE_RECORD_POOL_ENTRY
FUNC_8007A244=COMPLETE_RECORD_POOL_RESET_WORKER
FUNC_8007C444=COMPLETE_RECORD_WORD_ZERO_CLEAR_HELPER
PRODUCTION_REACHABILITY=blocked_at_func_801924F8_80192750_cut
NEXT_ARTIFACT_FREE_RUNG=movie_setup_from_0x80192750
```
