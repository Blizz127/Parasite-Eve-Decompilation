# PE-BTL69 — 66CE8 publishes `D_800BD000` each tick

`3F3C4` @ `3F560` jals `68CE0`, whose first jal is `66CE8`.
That is the walk heading matrix `78934` consumes. The 3F3C4
cut already took the `B0CD8&0x100/0x200` gate for `37870`;
`66CE8` is now the first jal on that gate. `68CE0` tail
(`65674` / `67E1C` / `67A78` / `67B74` / `67D18`) is not
this cut.

`66CE8` — 158 words `0x80066CE8..0x80066F60`, SHA-256
`a015062e…b0a9`. Digital yaw is `BD020`; analog `BD022`
when `BE9A0&0xF000==0x7000` is not this cut. `77DC4` /
`77CF4` are the table `rcos` / `rsin` leaves. Ry is stored
at `BD000`; `BD014/18/1C` are zeroed.

Do not invent pad. Idle `D26C=0` still skips 710A4/7136C.
