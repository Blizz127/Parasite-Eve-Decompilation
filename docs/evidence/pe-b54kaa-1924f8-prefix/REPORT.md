# PE-B54K-AA — `func_801924F8` indexed-record prefix

Status: **VERIFIED AND INTEGRATED ON THE NATIVE GRIND LANE**.

This rung authenticates the next overlay function and translates only its
29-word straight-line prefix. It stops at the first overlay-local call; no
later file, graphics, scheduler, story, or persistence behavior is guessed.

## Retail identity and function-hood

```text
PE.IMG SHA-1       146c0ce7308bf9fdc2ba5a84230e198db0663f3b
function           [0x801924F8,0x80192934)
size               0x43C / 271 words
SHA-256            ef825dccdbfd2a74941203d37739c713ad1e3bd8de48ca747f55d0e75a92f00a
return             0x8019292C jr ra / 0x80192930 nop
exact-start caller 0x80192E00 jal 0x801924F8
preceding boundary 0x801924F0 jr ra / 0x801924F4 nop
following boundary 0x80192934 lui v0,0x800B
translated prefix [0x801924F8,0x8019256C), 0x74 / 29 words
prefix SHA-256     2e6352856b04f1eee0ab1bae00324a36306fc63e14cb3101d030e073914b9ab2
next instruction  0x8019256C jal func_801918F8
```

Function-hood is therefore proven independently of the autogenerated name:
normal return, exact-start call, and real instructions on both boundaries.

## Prefix contract

The low half of the argument is validated as an unsigned index `< 47`. An
invalid index returns zero without mutation. A valid index stores its low byte
to `D_800B0DBF`, selects the 20-byte record at
`0x801D0E00 + index * 20`, publishes that pointer in `D_801D11AC`, and copies
record byte `+4` to `D_800B0DBB`. Retail then calls
`func_801918F8(0, (signed char)record[4])`; this exact two-register ABI is the
new boundary.

The complete function has 24 direct calls and substantial later file/GPU
control flow. None is approximated by this prefix.

## Verification

The existing real-disc B54K-Y contract now proves the canonical index-1 state
and exact `func_801918F8` arguments. Its negative arm also proves index 47 is
mutation-free. The suite remains 985 tests.

```text
normal full suite: 985 run, 985 passed, 0 failed, 0 skipped
fresh ASan/UBSan:  985 run, 985 passed, 0 failed, 0 skipped
strict frontier:   func_801918F8 called from func_801924F8
normal telemetry:  vsyncs=486 drawsyncs=1445 presents=483
DMA telemetry:     calls=27 queries=26 services=26 captured=26 serviced=26
EXE SHA-1:         452fb033f2eaa4b18aa20a5bca60b8125af3a37b
```

```text
FUNC_801924F8=AUTHENTICATED_271_WORDS_PREFIX_29_WORDS
PRODUCTION_REACHABILITY=blocked_at_func_801918F8_from_func_801924F8
SCHEDULER_PROVENANCE=NEEDS_ARTIFACT
NEXT_ARTIFACT_FREE_RUNG=audit_func_801918F8
```

Postscript: B54K-AB subsequently completed `func_801918F8` and advanced this
prefix to 35 words. The current boundary is the exact internal cut
`0x80192584`; the measurements above remain B54K-AA's original state.
